{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning",
      "System.Net.Http.HttpClient": "Warning"
    }
  },

  "API": {
    // Base URL of the API backend (Blazor WASM mode)
    // - On development environment, this value can be empty, then the BaseUrl is automatically obtained from the (WebAssemblyHostBuilder).HostEnvironment or NavigationManager. Or match this value with the one defined in the project AzPC.Blazor's Properties/launchSettings.json file.
    // - Or populate this value with the base URL of the API backend.
    "BaseUrl": ""
  }
}
